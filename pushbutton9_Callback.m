% --- ????????????.

function pushbutton9_Callback(hObject, eventdata, handles)

% hObject handle to pushbutton9 (see GCBO)

% eventdata reserved - to be defined in a future version of MATLAB

% handles structure with handles and user data (see GUIDATA)

global x1;

global Fs;

global derta_Fs;



[fn,pn,fi]=uigetfile('*.wav','select a wav-file')%????????????fn?????pn????

name=strcat(pn,fn)

[x1,Fs]=audioread(name);

sound(x1,Fs);%????

figure(1);

subplot(211);

plot(x1); %????????????

title('Origin Speech Signal');

xlabel('time n');

ylabel('voice n');

y1=fft(x1); %?length(x1)??FFT

y1=fftshift(y1);%?????????0

derta_Fs = Fs/length(x1);%??????????? ?

subplot(212);

plot([-Fs/2:derta_Fs: Fs/2-derta_Fs],abs(y1));%??????????????????x???????y?????

title('the frequency of origin speech signal');

grid on;